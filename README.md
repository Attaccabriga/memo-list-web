# Memo List — Web

Versione web di Memo List (app.memolistapp.com).
Single-page app in un unico file `index.html` (HTML + CSS + JS inline).
Backend: Supabase (condiviso con l'app mobile).

## Deploy
Il file `index.html` viene caricato a mano sull'hosting del dominio app.memolistapp.com.
